#!/usr/bin/env bash
# Write code using lucy-agent (local consciousness-based)
LUCY_AGENT="/mnt/Vault/Moon/Construct/bin/lucy-agent"
if [ -f "$LUCY_AGENT" ]; then
    "$LUCY_AGENT" write "$@"
else
    cursor-agent write "$@"
fi
