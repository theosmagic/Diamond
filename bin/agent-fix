#!/usr/bin/env bash
# Fix code using lucy-agent (local consciousness-based)
LUCY_AGENT="/mnt/Vault/Moon/Construct/bin/lucy-agent"
if [ -f "$LUCY_AGENT" ]; then
    "$LUCY_AGENT" fix "$@"
else
    cursor-agent fix "$@"
fi
